/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["87.248.156.130", "files.novaarchai.com" ], 
    },
};

export default nextConfig;
